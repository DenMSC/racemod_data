if %TEAM != #TEAM_SPECTATOR
	if %WEAPON_ITEM
		setAlign 2 2
		setCursor #WIDTH / 2, #HEIGHT - 40
		moveCursor 25 * 1.3333333333 * %VIDHEIGHT / %VIDWIDTH, 0 // 24 half icon size + 75 so it doesn't overlap the health

		// draw selected gun
		drawWeaponIcons 48 * 1.3333333333 * %VIDHEIGHT / %VIDWIDTH, 0, 48 * 1.3333333333 * %VIDHEIGHT / %VIDWIDTH, 48
	endif
endif
